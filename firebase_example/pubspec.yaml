name: firebase_example
description: A riverpod + Firebase + GoRouter integration example.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.2.0

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.9
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  flutter_riverpod: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
